athena {
  host = "http://localhost:8080/athena/api"

  project {
    code = "P1"
    name = "Project 1"
  }

  environment {
    code = "E1"
    name = "Environment 1"
  }

  pipeline {
    name = "Smoke Test"
    description = "Pipeline to execute smoke tests"
    number = "1"

    metadata = [
      {"name": "P1", "value": "V1"}
      {"name": "P2", "value": "V2"}
    ]

    execution.metadata = [
      {"name": "E1", "value": "V1"}
      {"name": "E2", "value": "V2"}
    ]

    executor.name = "akeshmiri"

  }
}